This is my original text

Hey this carpet is orange and grey, and there is a wrapper on the floor.

git init
to start your project

git add ______
to add your changes

git commit -m "comment"
to commit your changes

go to github and create a repository

git remote add origin<URL>

git push origin master

git add ____

git commit -m ""

subsequent pushes just
git push
